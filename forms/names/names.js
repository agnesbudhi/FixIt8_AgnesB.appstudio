/* Name the form 'namesExtra'. Using this array, print out each o f the  names using a for loop to cycle 
through the array starting at the end and moving to the front. You may not reorder the array. The output should look like this: backwards


let names = ['Bob', 'Janet', 'Tom', 'Bob', 'Randy', 'Elizabeth', 'Kim', 'Nancy']

for (let i = names.length - 1; i >=0 ; i--) {
    console.log(names[i]);
    }
*/