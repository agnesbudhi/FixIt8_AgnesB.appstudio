// Name the form 'whileExtra'. Create a program that gets a users' first and last name and cycles through the name one character at a time, outputting the letter in each iteration. 
/*
let name = prompt('What is your name?')
let letterNum = 1

for (let i = 0; i < name.length; i++) {
  console.log(`Letter ${letterNum} is: ${name.charAt(i)}`)
  letterNum++
  }
*/
